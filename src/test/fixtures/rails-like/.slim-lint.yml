# Deliberately stricter than the default so a test can prove this file was actually discovered:
# slim-lint only searches upward from the process cwd, never from the file path.
linters:
  LineLength:
    max: 40
